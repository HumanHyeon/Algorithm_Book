#include <string>
#include <vector>
#include <math.h>

using namespace std;

int solution(vector<vector<int>> triangle) {
    int answer = 0;
    int *cache = new int[pow(2, triangle.size()-1)];

    for (int i = 0; i < triangle.size(); i++)
    {
        for (int j = 0; j < triangle[i].size; j++)
        {
            /* code */
        }
        
    }
    

    return answer;
}