#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<int> solution(vector<int> progresses, vector<int> speeds) {
    vector<int> answer;
    int startLine = 0;
    bool finish[101] = { false, };

    while (true) {
        int count = 0;

        if (startLine == progresses.size()) break;
        for (int i = 0; i < progresses.size(); i++) {
            progresses[i] += speeds[i];
            if (progresses[i] > 99)  finish[i] = true;
        }
        for (int i = startLine; i < progresses.size(); i++) {
            if (finish[i] == false)  break;
            count++;
            startLine = i + 1;
        }
        if (count)   answer.push_back(count);
    }
    return answer;
}

int main() {
    vector<int> progresses;
    vector<int> speeds;
     
    int arr[] = { 93, 30, 55 };
    int arr2[] = { 1, 30, 5 };

    for (int i = 0; i < sizeof(arr) / sizeof(arr[0]); i++)
        progresses.push_back(arr[i]);
    for (int i = 0; i < sizeof(arr2) / sizeof(arr2[0]); i++)
        speeds.push_back(arr2[i]);

    vector<int> answer;
    answer = solution(progresses, speeds);
    
    for (int i = 0; i < answer.size(); i++)
        cout << answer[i] << " ";
    putchar('\n');
}

//Very impress
/*vector<int> solution(vector<int> progresses, vector<int> speeds) {
    vector<int> answer;

    int day;
    int max_day = 0;
    for (int i = 0; i < progresses.size(); ++i)
    {
        day = (99 - progresses[i]) / speeds[i] + 1;

        if (answer.empty() || max_day < day)
            answer.push_back(1);
        else
            ++answer.back();

        if (max_day < day)
            max_day = day;
    }

    return answer;
}*/
