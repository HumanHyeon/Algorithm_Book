#include <vector>
#include <cmath>

std::vector<int> solution(int brown, int yellow) {
    for (int i = 1; i <= sqrt(yellow); i++) 
            if (!(yellow % i) && (brown - (yellow / i + 2) * 2 - i * 2) == 0) return { yellow / i + 2, i + 2 };
}
