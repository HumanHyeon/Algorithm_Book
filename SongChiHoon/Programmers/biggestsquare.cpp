#include <iostream>
#include<vector>
using namespace std;

int squaresearch(vector<vector<int>> input, int startpoint) {
    int size;

    
    return size;
}

int solution(vector<vector<int>> board) {
    int answer = 1234;
    int vertical_length = board.size();
    int horizontal_length = board[0].size();

    for(int i = 0; i < vertical_length; i++) {
        board
    }

    // [실행] 버튼을 누르면 출력 값을 볼 수 있습니다.
    cout <<  << endl;

    return answer;
}