#include <string>
#include <vector>
#include   <iostream>
#include <algorithm>
using namespace std;

string solution(vector<string> participant,vector<string>completion){

    string answer = "";
    vector <string> participant2;
    vector <int> same_name(participant.size());
    vector <string> s;
    vector <int> comple_num(completion.size(), 1);
    int a = 0;

    for (int i = 0; i < participant.size(); i++)
    {
        for (int j = 0; j < completion.size(); j++)
        {
            if (participant[i] == completion[j])
            {
                if (comple_num[j] == 0) { ; }
                else          
                {comple_num[j]--;}
                same_name[i]++;
            }
        }
    }
    for (int i = 0; i < participant.size(); i++)
    {
        if (same_name[i] == 0)
        {
            answer = participant[i];
            cout << answer;
        }
    }
    return answer;
}    
